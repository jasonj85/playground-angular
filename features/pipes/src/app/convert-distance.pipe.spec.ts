import { ConvertDistancePipe } from './convert-distance.pipe';

describe('ConvertDistancePipe', () => {
  it('create an instance', () => {
    const pipe = new ConvertDistancePipe();
    expect(pipe).toBeTruthy();
  });
});
